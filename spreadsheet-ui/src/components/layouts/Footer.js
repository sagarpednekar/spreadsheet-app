import React, { Component } from 'react'

export default ()=> {
        return (
            <div>
                <footer class="container">
                    <p>&copy; AccionLabs { new Date().getFullYear() }</p>
                </footer>
            </div>
        )
}
